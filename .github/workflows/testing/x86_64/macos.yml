name: MacOS-X86_64-Test

permissions:
  contents: read

on:
  push:
  pull_request:

jobs:
  arm64:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: "Hello World"
        uses: "echo Hello World"