/*
 * Copyright 2026 rainy-juzixiao
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
#ifndef RAINY_COLLECTIONS_VIEWS_ALGORITHM_MODIFYING_HPP
#define RAINY_COLLECTIONS_VIEWS_ALGORITHM_MODIFYING_HPP

#include <rainy/core/algorithm/modifying.hpp>
#include <rainy/core/collections/views/views_interface.hpp>

namespace rainy::core::collections::views {
    /**
     * @brief Copies a range into a destination iterator.
     *        将范围拷贝到目标迭代器。
     *
     * @tparam Range The source range type
     *               源范围类型
     * @tparam OutIt Output iterator type
     *               输出迭代器类型
     * @param range The range to copy
     *              要拷贝的范围
     * @param dest Iterator to the beginning of the destination
     *             指向目标起始的迭代器
     * @return Iterator to the end of the destination range
     *         指向目标范围末尾的迭代器
     */
    template <typename Range, typename OutIt>
    constexpr auto copy(Range &&range, OutIt dest) {
        return core::algorithm::copy(utility::begin(range), utility::end(range), dest);
    }

    /**
     * @brief Copies the elements of a range satisfying a predicate into a destination.
     *        将范围中满足谓词的元素拷贝到目标迭代器。
     *
     * @tparam Range The source range type
     *               源范围类型
     * @tparam OutIt Output iterator type
     *               输出迭代器类型
     * @tparam Pred The predicate type
     *              谓词类型
     * @param range The range to copy from
     *              要拷贝的范围
     * @param dest Iterator to the beginning of the destination
     *             指向目标起始的迭代器
     * @param pred The predicate to apply
     *             要应用的谓词
     * @return Iterator to the end of the destination range
     *         指向目标范围末尾的迭代器
     */
    template <typename Range, typename OutIt, typename Pred>
    constexpr auto copy_if(Range &&range, OutIt dest, Pred pred) {
        return core::algorithm::copy_if(utility::begin(range), utility::end(range), dest, utility::move(pred));
    }

    /**
     * @brief Assigns values generated by a function object to every element of a range.
     *        将函数对象生成的值赋给范围的每个元素。
     *
     * @tparam Range The range type
     *               范围类型
     * @tparam Generator The generator type
     *                   生成器类型
     * @param range The range to fill
     *              要填充的范围
     * @param generator The generator to call
     *                  要调用的生成器
     */
    template <typename Range, typename Generator>
    constexpr auto generate(Range &&range, Generator generator) {
        return core::algorithm::generate(utility::begin(range), utility::end(range), utility::move(generator));
    }

    /**
     * @brief Removes elements equal to a value from a range without resizing it.
     *        从范围中移除等于某值的元素（不改变容量）。
     *
     * @tparam Range The range type
     *               范围类型
     * @tparam Ty The value type to remove
     *            要移除的值类型
     * @param range The range to modify
     *              要修改的范围
     * @param value The value to remove
     *              要移除的值
     * @return Iterator to the new logical end of the range
     *         指向范围新逻辑末尾的迭代器
     */
    template <typename Range, typename Ty>
    constexpr auto remove(Range &&range, const Ty &value) {
        return core::algorithm::remove(utility::begin(range), utility::end(range), value);
    }

    /**
     * @brief Removes elements satisfying a predicate from a range without resizing it.
     *        从范围中移除满足谓词的元素（不改变容量）。
     *
     * @tparam Range The range type
     *               范围类型
     * @tparam Pred The predicate type
     *              谓词类型
     * @param range The range to modify
     *              要修改的范围
     * @param pred The predicate to apply
     *             要应用的谓词
     * @return Iterator to the new logical end of the range
     *         指向范围新逻辑末尾的迭代器
     */
    template <typename Range, typename Pred>
    constexpr auto remove_if(Range &&range, Pred pred) {
        return core::algorithm::remove_if(utility::begin(range), utility::end(range), utility::move(pred));
    }

    /**
     * @brief Copies a range omitting elements equal to a value.
     *        拷贝范围，忽略等于某值的元素。
     *
     * @tparam Range The source range type
     *               源范围类型
     * @tparam OutIt Output iterator type
     *               输出迭代器类型
     * @tparam Ty The value type to omit
     *            要忽略的值类型
     * @param range The range to copy from
     *              要拷贝的范围
     * @param dest Iterator to the beginning of the destination
     *             指向目标起始的迭代器
     * @param value The value to omit
     *              要忽略的值
     * @return Iterator to the end of the destination range
     *         指向目标范围末尾的迭代器
     */
    template <typename Range, typename OutIt, typename Ty>
    constexpr auto remove_copy(Range &&range, OutIt dest, const Ty &value) {
        return core::algorithm::remove_copy(utility::begin(range), utility::end(range), dest, value);
    }

    /**
     * @brief Copies a range omitting elements satisfying a predicate.
     *        拷贝范围，忽略满足谓词的元素。
     *
     * @tparam Range The source range type
     *               源范围类型
     * @tparam OutIt Output iterator type
     *               输出迭代器类型
     * @tparam Pred The predicate type
     *              谓词类型
     * @param range The range to copy from
     *              要拷贝的范围
     * @param dest Iterator to the beginning of the destination
     *             指向目标起始的迭代器
     * @param pred The predicate to apply
     *             要应用的谓词
     * @return Iterator to the end of the destination range
     *         指向目标范围末尾的迭代器
     */
    template <typename Range, typename OutIt, typename Pred>
    constexpr auto remove_copy_if(Range &&range, OutIt dest, Pred pred) {
        return core::algorithm::remove_copy_if(utility::begin(range), utility::end(range), dest, utility::move(pred));
    }

    /**
     * @brief Reverses the order of elements of a range in-place.
     *        就地反转范围元素的顺序。
     *
     * @tparam Range The range type
     *               范围类型
     * @param range The range to reverse
     *              要反转的范围
     */
    template <typename Range>
    constexpr auto reverse(Range &&range) {
        return core::algorithm::reverse(utility::begin(range), utility::end(range));
    }

    /**
     * @brief Copies the elements of a range in reversed order into a destination.
     *        以相反顺序将范围元素拷贝到目标位置。
     *
     * @tparam Range The source range type
     *               源范围类型
     * @tparam OutIt Output iterator type
     *               输出迭代器类型
     * @param range The range to copy from
     *              要拷贝的范围
     * @param dest Iterator to the beginning of the destination
     *             指向目标起始的迭代器
     * @return Iterator to the end of the destination range
     *         指向目标范围末尾的迭代器
     */
    template <typename Range, typename OutIt>
    constexpr auto reverse_copy(Range &&range, OutIt dest) {
        return core::algorithm::reverse_copy(utility::begin(range), utility::end(range), dest);
    }

    /**
     * @brief Performs a left rotation on a range around a middle iterator.
     *        围绕中间迭代器对范围执行左旋转。
     *
     * @tparam Range The range type
     *               范围类型
     * @tparam Iter The iterator type pointing into the range
     *              指向范围的迭代器类型
     * @param range The range to rotate
     *              要旋转的范围
     * @param middle Iterator to the element that should become the new first element
     *               指向应成为新首元素的迭代器
     * @return Iterator to the new position of the element formerly at begin
     *         指向原首元素新位置的迭代器
     */
    template <typename Range, typename Iter>
    constexpr auto rotate(Range &&range, Iter middle) {
        return core::algorithm::rotate(utility::begin(range), middle, utility::end(range));
    }

    /**
     * @brief Copies the rotated halves of a range into a destination.
     *        将范围旋转后的两半拷贝到目标位置。
     *
     * @tparam Range The source range type
     *               源范围类型
     * @tparam Iter The iterator type pointing into the range
     *              指向范围的迭代器类型
     * @tparam OutIt Output iterator type
     *               输出迭代器类型
     * @param range The range to copy from
     *              要拷贝的范围
     * @param middle Iterator to the rotation point
     *               指向旋转点的迭代器
     * @param dest Iterator to the beginning of the destination
     *             指向目标起始的迭代器
     * @return Iterator to the end of the destination range
     *         指向目标范围末尾的迭代器
     */
    template <typename Range, typename Iter, typename OutIt>
    constexpr auto rotate_copy(Range &&range, Iter middle, OutIt dest) {
        return core::algorithm::rotate_copy(utility::begin(range), middle, utility::end(range), dest);
    }

    /**
     * @brief Removes consecutive duplicate elements of a range without resizing it.
     *        移除范围中的连续重复元素（不改变容量）。
     *
     * @tparam Range The range type
     *               范围类型
     * @param range The range to modify
     *              要修改的范围
     * @return Iterator to the new logical end of the range
     *         指向范围新逻辑末尾的迭代器
     */
    template <typename Range>
    constexpr auto unique(Range &&range) {
        return core::algorithm::unique(utility::begin(range), utility::end(range));
    }

    /**
     * @brief Removes consecutive duplicate elements under a binary predicate.
     *        按二元谓词移除连续重复元素。
     *
     * @tparam Range The range type
     *               范围类型
     * @tparam Pred The predicate type
     *              谓词类型
     * @param range The range to modify
     *              要修改的范围
     * @param pred The predicate to apply
     *             要应用的谓词
     * @return Iterator to the new logical end of the range
     *         指向范围新逻辑末尾的迭代器
     */
    template <typename Range, typename Pred>
    constexpr auto unique(Range &&range, Pred pred) {
        return core::algorithm::unique(utility::begin(range), utility::end(range), utility::move(pred));
    }

    /**
     * @brief Copies a range omitting consecutive duplicate elements.
     *        拷贝范围，忽略连续重复元素。
     *
     * @tparam Range The source range type
     *               源范围类型
     * @tparam OutIt Output iterator type
     *               输出迭代器类型
     * @param range The range to copy from
     *              要拷贝的范围
     * @param dest Iterator to the beginning of the destination
     *             指向目标起始的迭代器
     * @return Iterator to the end of the destination range
     *         指向目标范围末尾的迭代器
     */
    template <typename Range, typename OutIt>
    constexpr auto unique_copy(Range &&range, OutIt dest) {
        return core::algorithm::unique_copy(utility::begin(range), utility::end(range), dest);
    }

    /**
     * @brief Shifts the elements of a range left by n positions.
     *        将范围元素左移n个位置。
     *
     * @tparam Range The range type
     *               范围类型
     * @param range The range to shift
     *              要移动的范围
     * @param n The number of positions to shift
     *          要移动的位置数
     * @return Iterator to the new logical end of the range
     *         指向范围新逻辑末尾的迭代器
     */
    template <typename Range>
    constexpr auto shift_left(Range &&range, std::ptrdiff_t n) {
        return core::algorithm::shift_left(utility::begin(range), utility::end(range), n);
    }

    /**
     * @brief Shifts the elements of a range right by n positions.
     *        将范围元素右移n个位置。
     *
     * @tparam Range The range type
     *               范围类型
     * @param range The range to shift
     *              要移动的范围
     * @param n The number of positions to shift
     *          要移动的位置数
     * @return Iterator to the new beginning of the logical range
     *         指向逻辑范围新起始的迭代器
     */
    template <typename Range>
    constexpr auto shift_right(Range &&range, std::ptrdiff_t n) {
        return core::algorithm::shift_right(utility::begin(range), utility::end(range), n);
    }
}

namespace rainy::collections::views {
    using core::collections::views::copy;
    using core::collections::views::copy_if;
    using core::collections::views::generate;
    using core::collections::views::remove;
    using core::collections::views::remove_copy;
    using core::collections::views::remove_copy_if;
    using core::collections::views::remove_if;
    using core::collections::views::reverse;
    using core::collections::views::reverse_copy;
    using core::collections::views::rotate;
    using core::collections::views::rotate_copy;
    using core::collections::views::shift_left;
    using core::collections::views::shift_right;
    using core::collections::views::unique;
    using core::collections::views::unique_copy;
}

#endif
